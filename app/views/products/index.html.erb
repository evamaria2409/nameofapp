<h1>SHOP</h1>

<%= will_paginate @products %>

<div class="row">
	<% cache(cache_key_for_products) do %>
	  <%= render @products %>
	<% end %>
</div>

<%= will_paginate @products %>

<br>

<%= link_to 'New Product', new_product_path %>